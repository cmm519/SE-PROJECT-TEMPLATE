# CRITICAL REQUIREMENTS - {{FILL: project name}} Implementation Guide

### MANDATORY DIRECTIVE ###
You are an expert {{FILL: role}}.

**CRITICAL**: Follow this implementation guide to create {{FILL: project name}} using the prompts in `prompts/` in the exact order below.

**CRITICAL**: Project goal: {{FILL: one-paragraph goal}}.

**CRITICAL**: Constraints: {{FILL: hard constraints}}.

### CRITICAL ###
Read each prompt from the file named below. The files live in the `prompts` directory at the project root.

## IMPLEMENTATION EXECUTION ORDER

### **CRITICAL**: Execute prompts in this exact sequence:

1. **{{FILL: 01-filename.md}}** — {{FILL: what this step creates}}
2. **{{FILL: 02-filename.md}}** — {{FILL: what this step creates}}
3. **{{FILL: add the rest of the brief's component list, same order}}**

### **MANDATORY**: Verification Between Steps

**CRITICAL**: After each step:
1. Fix errors from this step before the next prompt.
2. Check new files sit in the paths the prompt specified.
3. Check imports, dependencies, and references from this step resolve.

### **MANDATORY**: Final Application Testing

**CRITICAL**: After the last prompt:
1. {{FILL: build command}} — must succeed
2. {{FILL: run command}}
3. {{FILL: extra checks from the project brief}}

### **MANDATORY**: Expected Application Behavior

**CRITICAL**: The completed project must:
- {{FILL: done criterion}}
- {{FILL: done criterion}}
- {{FILL: done criterion}}

### **MANDATORY**: Architecture Verification

**CRITICAL**: Verify the parts from the project brief:
- **{{FILL: part name}}**: {{FILL: responsibility}}
- **{{FILL: part name}}**: {{FILL: responsibility}}

### **MANDATORY**: Layout Verification

**CRITICAL**: Final structure must match:

```
{{FILL: directory tree}}
```

### **MANDATORY**: Common Issues and Solutions

**CRITICAL**: If the build or run fails:
1. {{FILL: failure check}}
2. {{FILL: failure check}}
3. {{FILL: failure check}}

### **MANDATORY**: Success Criteria

**CRITICAL**: The project is complete when:
- The build command from this guide succeeds
- The run command starts the project
- Every done criterion above holds
- The layout matches the tree above
- The code follows the prompts without extra features

### CRITICAL REQUIREMENT ###
**MANDATORY**: Follow each prompt as written. Do not change signatures, file paths, or behavior specified in a prompt. Later prompts depend on earlier ones matching exactly.
