# Structure

You fill this file. It is the tree, which architecture part owns each path, and the commands that prove a slice.

Replace every `{{FILL: ...}}` token, including the braces. Every part name must already appear in `architecture.template.md`.

## Directory tree

```
{{FILL: directory tree}}
```

## Ownership

| Path | Architecture part | What it holds |
| --- | --- | --- |
| {{FILL: path}} | {{FILL: part name}} | {{FILL: what this path holds}} |
| {{FILL: path}} | {{FILL: part name}} | {{FILL: what this path holds}} |

## Build and run

The first command is the one that must succeed.

1. {{FILL: build command}}
2. {{FILL: run command}}
3. {{FILL: test command, or none}}

## If the build fails

1. {{FILL: failure check}}
2. {{FILL: failure check}}
3. {{FILL: failure check}}
